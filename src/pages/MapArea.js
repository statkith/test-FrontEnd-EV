import React from "react";
import CardList from "../components/CardList";

const MapArea = (props) => {
  return (
    <div className="map-container" style={{ height: "93vh" }}>
      <CardList />
    </div>
  );
};

export default MapArea;
